schema: rawdata
table1:
  tablename: 'hello_world'
  query:
    variable:
      var1: 'var1_customer'
      var2: 'var2_order'
    condition:
      date: 2022-07-09

# pkg:
#   python:
#     version: 3.6.8
#     date: "{{ date }}"
#   django:
#     version: "{% if pkg.python.version|first == '2' %}1.8{% else %}2.2.6{% endif %}"
